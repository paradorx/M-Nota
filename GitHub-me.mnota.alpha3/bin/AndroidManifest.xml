<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.mnota.alpha2"
    android:versionCode="1"
    android:versionName="1.0" >

    <!-- android:minSdkVersion="7" is 2.2, android:targetSdkVersion="15" is 4.0.3 -->
    <uses-sdk android:minSdkVersion="7" android:targetSdkVersion="15"/>

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Sherlock" >
        	<!-- ^above^ set up theme to refers to GitHub-ActionBarSherlock -->
        <activity
            android:name=".HomeMenu"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="List_note_TitlesWithListener"></activity>
        <activity android:name="viewfakulti"></activity>
        <activity android:name="viewprogram"></activity>
        <activity android:name="viewsubjek"></activity>
        <activity android:name="Menu_tetapan"></activity>
    </application>

</manifest>